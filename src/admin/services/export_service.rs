// src/admin/services/export_service.rs

// Export service for business logic
// Currently, export logic is in handlers, but this can be refactored here if needed

pub struct ExportService;

impl ExportService {
    pub fn new() -> Self {
        Self
    }
}
