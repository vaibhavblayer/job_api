// src/admin/services/dashboard_service.rs

// Dashboard service for business logic
// Currently, dashboard logic is in handlers, but this can be refactored here if needed

pub struct DashboardService;

impl DashboardService {
    pub fn new() -> Self {
        Self
    }
}
